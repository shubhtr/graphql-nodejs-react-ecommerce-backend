# graphql-nodejs-react-ecommerce-backend

This repository contains the source code for NodeJS backend for an ECommerce application. It uses GraphQL to work with the ReactJS frontend.

GraphQL, NodeJS

# Setup

